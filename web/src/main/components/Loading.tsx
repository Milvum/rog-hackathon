import * as React from 'react';

export default class Loading extends React.PureComponent {
    public render() {
        return (
            <div>
                <p>Loading...</p>
            </div>
        );
    }
}
